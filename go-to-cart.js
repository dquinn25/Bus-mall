var array = [1, 2, 3, 4, 5]; 
function returnTopChoice(){
    return clicked
    //change back to image if it works

    var topChoice = array[0];
    //console.log(Math.max(...array1));
    
    for(i = 0; i < array.length; i++) {
        topChoice.clicked < array[i].clicked
        console.log(Math.max(array[i]));
    }
}
        
        //if dthe choice is not higher than the other than that would return top choice
//for loop for all values
//each time have them compare to the other one
//loopeed list